using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CpuTargetsManager : MonoBehaviour
{
    [SerializeField] List<Transform> trackWayPoints;
    public List<Transform> TrackWayPoints { get{ return trackWayPoints;}}
    // Start is called before the first frame update
    void Start()
    {
        
    }
    

    // Update is called once per frame
    void Update()
    {
        
    }
}
